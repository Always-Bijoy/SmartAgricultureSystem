import 'package:flutter/material.dart';


class RoundedButton extends StatelessWidget {
    final String title;
    final Color color;

  const RoundedButton({ this.title, this.color}) ;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
